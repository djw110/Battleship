/*
 * Used to determine a Ship's Type when performing operations on a fleet
 * by a board without needing to reference an individual ship object
 */
public enum ShipType {
    ST_AIRCRAFT_CARRIER, ST_BATTLESHIP, ST_CRUISER, ST_SUB, ST_DESTROYER;
}